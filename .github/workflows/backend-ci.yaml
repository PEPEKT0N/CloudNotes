name: Backend CI

on:
  pull_request:
    branches: [ develop, main ]
  push:
    branches: [ develop ]

jobs:
  restore-build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-packages-${{ hashFiles('**/*.csproj') }}
          restore-keys: |
            nuget-packages-

      - name: Restore
        run: dotnet restore CloudNotes.sln

      - name: Run dotnet format (check only)
        run: dotnet tool restore && dotnet format CloudNotes.sln --verify-no-changes 

      - name: Build
        run: dotnet build CloudNotes.sln --no-restore -c Release

      - name: Test
        run: dotnet test CloudNotes.sln --no-build -c Release
